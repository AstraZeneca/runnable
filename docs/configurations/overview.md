**runnable** is designed to enable the pipeline execution in varied computational environments.

This is controlled by a ```configuration``` file without changing either the domain code or the pipeline definition.
